# Task-packet: <task-slug>

<!-- Самодостаточное ТЗ на одну Task. Заполняется architect'ом из stories.md + tech-spec + PRD.
     Исполнитель видит ТОЛЬКО этот файл и репозиторий кода — ни PRD, ни переписки у него нет.
     Протокол — ../executor-protocol.md. Объём 150–250 строк; больше — режь Task надвое. -->

- **Эпик:** <epic-slug> · **Story:** <story-slug> · **Task:** <task-slug>
- **Ветка:** `task/<epic-slug>/<task-slug>` (от Story-ветки `feature/<epic-slug>/<story-slug>`)
- **Слой:** <domain | use-case | port | infra | ui | migration | infra-script>
- **Зависит от:** <task-slug'и из deps — их код уже в ветке> | нет
- **Пакет собрал:** <модель/дата> · **Исполнитель:** <inherit | sonnet | external:...>

## Цель

<Одна строка. Что появится в коде после этой Task. Без «почему» и без контекста продукта.>

## AC этой Task

<2–5 проверяемых утверждений УРОВНЯ TASK, не Story. Каждое либо покрыто красным тестом,
либо проверяется командой. Формулировка «когда <вход>, то <выход>».>

1. Когда ..., то ...
2. Когда ..., то ...
3. ...

## Контракт интерфейсов

<Готовые сигнатуры — не «спроектируй». Исполнитель наполняет тела, имена и типы менять нельзя.
Если Task создаёт публичный интерфейс, от которого зависят соседние Task — секция обязательна.>

```ts
// src/<feature>/domain/<name>.ts — СОЗДАТЬ
export type <Name> = {
  <field>: <type>   // <что означает, единицы, допустимые значения>
}

// src/<feature>/ports/<name>-port.ts — СОЗДАТЬ
export interface <Name>Port {
  <method>(<arg>: <Type>): Promise<<Result>>
}

// src/<feature>/use-cases/<name>.ts — СОЗДАТЬ
export async function <name>(
  deps: { <port>: <Name>Port },
  input: <Input>
): Promise<<Output>>
```

**Уже существует, использовать как есть** (не переписывать, не дублировать):

| Что | Где | Как звать |
|---|---|---|
| `<функция/тип>` | `<путь>` | `<пример вызова>` |

**Ошибки:** <перечень кодов/типов ошибок, которые обязан возвращать этот код, и когда именно.
Коды-строки перечисляй дословно — расхождение с транспортом ловится только в интеграции.>

## Разрешено править

<Точный список. Всё остальное в репозитории — read-only. Проверяется скриптом по git diff.
Пути — от корня git-репозитория. Тег блока ```allowed-paths обязателен: по нему читает VERIFY_CMD.
Допустим glob (`src/<feature>/domain/*.ts`); строки с `#` — комментарии.>

```allowed-paths
src/<feature>/domain/<name>.ts
src/<feature>/use-cases/<name>.ts
src/<feature>/__tests__/<name>.test.ts
```

**Явно read-only** (частая ошибка): `src/lib/db/schema.ts`, `drizzle/`, `<preview-эталон>`,
файлы соседних Task из той же parallel group.

## Красные тесты

<Тесты написаны architect'ом ДО реализации и сейчас падают. Задача — сделать их зелёными,
не меняя их текст. Для чистого UI вместо тестов — visual baseline и ссылка на preview-экран.>

| Файл теста | Что проверяет | Сейчас |
|---|---|---|
| `<путь>.test.ts` | AC-1, AC-2 | падает: `<текст ошибки>` |
| `<путь>.test.ts` | edge case: <какой> | падает |

Запуск только этих тестов (тег блока ```test-cmd обязателен — по нему читает `VERIFY_CMD`):

```test-cmd
<команда точечного прогона, напр. npx vitest run <путь>>
```

## Команда проверки

<Точечная, а не весь LOCAL_CHECK — он на большом проекте идёт минуты и не даёт сигнала по Task.>

```bash
<VERIFY_CMD из project-config> <task-slug>
```

Вердикт: exit 0 — готово; иначе в stdout JSON с причиной. Проверяет: красные тесты позеленели ·
typecheck чист · diff не вышел за «Разрешено править».

## Выдержки из спеки

<Скопированные абзацы, а не ссылки на 100-КБ файлы. Только то, что относится к этой Task.
Каждая выдержка помечена источником, чтобы можно было поднять оригинал при споре.>

**Из `tech-spec.md` §<номер> «<заголовок>»:**

> <дословная выдержка>

**Из `PRD.md`, AC-<N>:**

> <дословная выдержка>

**Из решений владельца / арх-ревью** (если Task их касается):

> <дословно: что решено и что этим отменено>

## Правила проекта, применимые здесь

<Выжимка из CLAUDE.md проекта — 3–7 строк, только релевантное. Не пересказ всего файла.>

- Архитектура: `domain/` без зависимостей · `use-cases/` зависят от портов · `infra/` реализует порты.
- <паттерн-образец: «делай как в src/auth/», если он есть>
- <анти-образец: «src/lib/actions/*.ts — legacy, не копировать»>
- Тесты: <раннер>, файлы рядом в `__tests__/`.

## Запреты

1. Не править тест, чтобы он позеленел. Тест неверен — стоп, `needs_decision` с обоснованием.
2. Не менять сигнатуры, типы и имена из «Контракта интерфейсов».
3. Не трогать миграции и схему БД.
4. Не «улучшать» preview-эталон — расхождение с ним есть дефект реализации.
5. Не править файлы вне «Разрешено править», даже очевидные опечатки.
6. Никаких `--no-verify`, `.skip`/`.only`, `@ts-ignore`, закомментированных проверок.
7. Не добавлять зависимости.
8. Не коммитить в `main` и не мержить ветки — только свой коммит в свою ветку.

## Лимиты

- Попыток авто-fix по красно-белому сигналу: **3**. Исчерпаны — стоп, `needs_decision`.
- Упёрся в противоречие спеки — не выбирай сам, верни `needs_decision` с описанием развилки.

## Формат ответа

Последним блоком, без текста после него:

```json
{
  "status": "ok | needs_approve | blocked | needs_decision",
  "task": "<task-slug>",
  "branch": "task/<epic-slug>/<task-slug>",
  "sha": "<sha коммита>",
  "files": ["<изменённые файлы>"],
  "not_done": ["<что осталось и почему>"],
  "summary": "<1–2 строки>"
}
```

---

<!-- ЧЕК-ЛИСТ ARCHITECT'А перед выдачей пакета (удалить из готового пакета):
     [ ] Исполнитель без PRD/tech-spec/переписки выполнит Task без единого вопроса?
     [ ] Каждый AC проверяем машиной?
     [ ] Красные тесты реально падают сейчас (прогнал и увидел)?
     [ ] Все имена, от которых зависят соседние Task, зафиксированы в контракте?
     [ ] Список «Разрешено править» точный — не шире и не уже реальной зоны?
     [ ] Выдержки из спеки скопированы, а не заменены ссылкой?
     [ ] Объём 150–250 строк? -->
