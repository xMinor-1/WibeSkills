<!--
Шаблон README handoff-пакета. Эталон — Продукты/Сайт х5т/Теплокарта грузов/freight-map-handoff/README.md
Заполняй секции под конкретный эпик. Пустые секции удаляй, не оставляй заглушки.
Цель: программист ставит и доводит до прода БЕЗ автора пакета.
-->

# <Название> — handoff для разработчика

<Одно-два предложения: что это за интеграция/фича и на какой стек ложится.>

**Безопасность:**
- <ключевые инварианты: где живут токены, как авторизация, что закрыто от Public>
- <XSS/инъекции: как экранируется пользовательский ввод>
- <чего НЕЛЬЗЯ делать — напр. токен в браузер, innerHTML>

---

## Что внутри

```
<slug>-handoff/
├── .env.example                # env-переменные с комментариями
├── README.md                   # этот файл
├── src/...                     # каркас кода (с TODO там, где нужна доводка)
└── strapi/                     # если есть бэкенд: schema.json + настройка
```

---

## Установка

### 1. Скопируй файлы в свой проект
<куда что класть; что делать при конфликте с существующими файлами (middleware и т.п.)>

### 2. Зависимости
```bash
<npm install ...>
```

### 3. Заполни `.env`
```bash
cp .env.example .env
# <как сгенерировать секреты, напр. openssl rand -hex 32>
```
<какие переменные обязательны, что будет если не хватает>

### 4. Бэкенд / Strapi (если нужно)
<content type, права Public = ничего, какой API-токен, куда положить>

### 5. Запусти
```bash
<npm run dev → какой URL, какой ожидаемый флоу>
```

---

## Слияние с существующим кодом
<если у программиста уже есть middleware/конфиг/роуты — что слить, а не перезаписывать; готовые сниппеты>

---

## Архитектурные решения (FYI)
<для каждого неочевидного решения — короткий абзац «почему так и когда менять».
Это снимает у программиста половину вопросов. Примеры из эталона:
- почему именно такой рендер / структура компонентов
- почему cookie+HMAC, а не JWT
- почему данные импортятся, а не fetch'атся
- какие упрощения сознательно приняты и при каком росте их пересматривать>

---

## Тесты после установки
<нумерованный чек-лист ручных проверок: auth-gate, happy path, токен не в браузере,
негативные кейсы (401/403), и т.д. — как в эталоне freight-map>

---

## Что НЕ сделано (вне scope)
<честный список: ротация токена, audit log, реальные пользователи, CSP-заголовки и т.п. —
чтобы программист знал границы пакета и не искал несуществующее>
