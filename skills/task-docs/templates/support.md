# Support: <Название фичи>

## Архитектура
- Слои: <UI компоненты → server actions / use-cases → repository → таблица>
- Ключевые файлы: file:line links
- Внешние зависимости: ...

## Поток данных
По AC из PRD — что происходит на каждом шаге технически.

## Точки отказа (failure modes)
| Симптом | Где смотреть | Как чинить |
|---|---|---|
| Пользователь не видит X | логи `<место>`, таблица `<name>` | <действие> |
| Аналитика не получает события | проверь `TRACK_HELPER` в `<место>` | <действие> |
| ... | ... | ... |

## Логи и метаданные
Какие действия логируются, в какую таблицу, как читать (если LEGAL_REGIME требует audit).

## Персональные данные
Какие поля = ПДн, как удалять по запросу субъекта (если LEGAL_REGIME задан).

## Известные ограничения / технический долг
То, что в `arch-review.md` пометили как «сознательный долг».

## Связи
- PRD: DOCS_ROOT/<epic-slug>/PRD.md
- Tech Spec: DOCS_ROOT/<epic-slug>/tech-spec.md
- Arch Review: DOCS_ROOT/<epic-slug>/arch-review.md
- Бэклог: DOCS_ROOT/<epic-slug>/stories.md
