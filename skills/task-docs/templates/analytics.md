# Analytics: <Название фичи>

## События (goals)
| event_name | Триггер | Где вызывается | Параметры | Бизнес-смысл |
|---|---|---|---|---|
| `<name>` | клик/submit/error | `<file>:42` | `{ id, type }` | Зачем смотрим |

## Что смотреть в отчётах
- Goals: какие конверсии важны
- Поведенческие записи (Webvisor / session replay): на что обращать внимание
- Heatmaps: где ожидаем активность, что считается аномалией

## Гипотезы для проверки
Что хотим узнать из данных по этой фиче — задаёт работу для `analytics-insights` через ~1–2 недели после релиза.
